%h1 Reservations

%table.table.table-sm
  %thead
    %tr
      %th Name
      %th Email
      %th Phone
      %th Street
      %th City
      %th
      %th
      %th

  %tbody
    - @reservations.each do |reservation|
      %tr
        %td= link_to reservation.name, admin_reservation_path(reservation)
        %td= reservation.email
        %td= reservation.phone
        %td= reservation.street
        %td= reservation.city

        %td= link_to 'Show', admin_reservation_path(reservation)
        %td= link_to 'Edit', edit_admin_reservation_path(reservation)
        %td= link_to 'Destroy', reservation, method: :delete, data: { confirm: 'Are you sure?' }

%br


= pagy_bootstrap_nav(@pagy).html_safe

