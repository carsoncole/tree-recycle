= render 'shared/reservation_header'

.row
  .col-md-12
    %p.text-muted Provide the address where the tree will be located and any helpful notes that describe where we will find the tree, such as your porch, or driveway.

    %p.text-muted.mb-5 We start pickups at 8am, so have the tree ready, with all decorations removed as they would not be compostable.


= render 'form'

